[package]
name = "agnoshi"
version = "0.90.0"
description = "AI-native natural language shell for AGNOS"
license = "GPL-3.0-only"
language = "cyrius"
cyrius = "4.2.5"

[build]
entry = "src/main.cyr"
output = "agnoshi"

[deps]
stdlib = ["string", "fmt", "alloc", "vec", "str", "syscalls", "io", "fs", "json", "chrono", "hashmap", "args", "tagged", "process", "fnptr", "net", "sakshi", "assert", "bench"]
